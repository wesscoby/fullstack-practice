type User {
    _id: ID
    firstName: String
    lastName: String
    email: String
}

type AuthPayload {
    user: User
}


type Query {
    currentUser: User
    user(email: String): User!
    users: [User!]!
}

type Mutation {
    login(email: String!, password: String!): AuthPayload
    logout: Boolean
}